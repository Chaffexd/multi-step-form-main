import React from 'react';
import PrevStep from '../UI/PrevStep';

const Summary = (props) => {
  return (
    <div>
      <p>Summary</p>
      <PrevStep {...props} />
    </div>
  );
};

export default Summary;